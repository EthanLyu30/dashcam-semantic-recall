# 开发技巧

这个文件替代原来的 `CLAUDE.md`，用于记录本项目的 AI 辅助开发约定。

## 先对齐原型

- 桌面端设计必须优先搬运 `附件2_桌面端交互原型_DVR-Semantic` 中已经完成的页面结构、导航命名、布局比例、色彩和交互状态。
- 不允许另起一套视觉语言。若 Qt6 实现受组件限制，只能做等价转译，并在 `docs/prototype-migration.md` 中说明差异。
- 页面命名沿用原型：系统状态概览、语义检索中心、视频库管理、人工复核中心、告警管理中心、事故摘要预览、证据与日志归档、全天业务报告、模型与安全配置、角色与权限管理、系统登录。

## 编码前先判断边界

- 吕霄阳侧负责客户端、页面转译、播放器跳转、结果展示、时间轴与前后端联调。
- 倪羽辰侧负责后端、视频处理、模型分析、检索、数据库、证据导出、日志和接口测试。
- 共享字段先改 `docs/api-contract.md`，再改客户端模型和后端 schema。

## 保持简单

- 先完成能演示的主链路：上传/导入 -> 处理状态 -> 语义检索 -> 点击回放 -> 导出证据。
- 不为暂时用不到的 GIS、工单流转、多视频联合检索提前写复杂框架。
- 后端服务优先写成可测试的纯函数和薄 API 层，方便倪羽辰逐步替换 mock。

## 每次改动都要有验收点

1. UI 改动：能对应到原型页面和原型控件。
2. API 改动：接口文档、schema、mock 响应一致。
3. 搜索改动：至少覆盖剐蹭、违停、道路障碍/异常停车中的一种测试。
4. 回放改动：选择事件后必须产生 `videoId + startSec + endSec` 的跳转数据。
5. 导出改动：必须产生导出记录，并能返回导出状态或错误原因。
